<UserControl x:Class="WindowsClientCenter.Plugins.DeviceActions.UI.DeviceActionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="SelectableValueTextBoxStyle"
               TargetType="TextBox"
               BasedOn="{StaticResource AppReadOnlyTextBoxStyle}" />
        <Style TargetType="Button" BasedOn="{StaticResource AppCommandButtonStyle}" />
        <Style TargetType="GroupBox" BasedOn="{StaticResource AppGroupBoxStyle}" />
        <Style TargetType="DataGrid" BasedOn="{StaticResource AppDataGridStyle}">
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="SelectionUnit" Value="FullRow" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <WrapPanel Margin="0,0,0,10">
            <Button Style="{StaticResource AppIconTextButtonStyle}" Tag="&#xE72C;" Content="Refresh" Width="110" Command="{Binding RefreshCommand}" />
            <Button Style="{StaticResource AppDangerIconTextButtonStyle}" Tag="&#xE7E8;" Content="Shutdown" Width="110" Command="{Binding ShutdownCommand}" />
            <Button Style="{StaticResource AppIconTextButtonStyle}" Tag="&#xE777;" Content="Restart" Width="110" Command="{Binding RestartCommand}" />
            <Button Style="{StaticResource AppIconTextButtonStyle}" Tag="&#xE7E9;" Content="Log off" Width="110" Command="{Binding LogoffCommand}" />
            <Button Style="{StaticResource AppIconTextButtonStyle}" Tag="&#xE72E;" Content="Lock workstation" Width="150" Command="{Binding LockWorkstationCommand}" />
            <Button Style="{StaticResource AppPrimaryIconTextButtonStyle}" Tag="&#xE945;" Content="Apply selected power plan" Width="210" Command="{Binding ApplySelectedPowerSchemeCommand}" />
        </WrapPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="Summary" Padding="8" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="170" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Host" FontWeight="SemiBold" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding HostText, TargetNullValue=-}" Style="{StaticResource SelectableValueTextBoxStyle}" />
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Active power plan" FontWeight="SemiBold" />
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ActiveSchemeText, TargetNullValue=-}" Style="{StaticResource SelectableValueTextBoxStyle}" />
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Warnings" FontWeight="SemiBold" />
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding WarningsText}" Style="{StaticResource SelectableValueTextBoxStyle}" AcceptsReturn="True" MinHeight="54" />
                    </Grid>
                </GroupBox>

                <GroupBox Header="Power Schemes" Padding="8" Margin="0,0,0,10">
                    <DataGrid ItemsSource="{Binding PowerSchemes}"
                              SelectedItem="{Binding SelectedPowerScheme, Mode=TwoWay}"
                              Height="260">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="GUID" Binding="{Binding SchemeId}" Width="260" />
                            <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70" />
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <TextBox Grid.Row="2"
                 Text="{Binding Status}"
                 Style="{StaticResource SelectableValueTextBoxStyle}" />
    </Grid>
</UserControl>
